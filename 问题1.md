项目重构问题列举如下：
1. 你是否理解我的工作？我做了102+2 = 104个物种，zws shrew_mole都是要参与评估的，我怎么可能过滤掉？
参考/home/rsun@ZHANGroup.local/hqy_new/CEP_project/src/leave_one_eval.py，
```python
filter zws and shrew_mole and leave_species'''
idx = meta_df.index.isin(['ZWS', 'shrew_mole', leave_species]) 
meta_df = meta_df.loc[~idx, :]
```
这肯定有问题的。
2. 我的 /home/rsun@ZHANGroup.local/hqy_new/CEP_project/cep_draft_0523.pdf 中说明了我是用RF做base model的吧，
/home/rsun@ZHANGroup.local/hqy_new/CEP_project/src/leave_one_eval.py
```python
def prediction(data_dic, leave_species, ref_list, dist_list, start_k=50, delta=50, steps=100):

    feature_df, test_feature, meta_df, plot_df, summary_df = (
        data_dic['feature_df'], data_dic['test_feature'],
        data_dic['meta_df'], data_dic['plot_df'], data_dic['summary_df']
    )

    '''补丁四，针对鲸鱼和蝙蝠'''
    # 对蝙蝠和齿鲸这两个有代表性的回声非回声物种，执行朴素贝叶斯模型 
    tmp_meta = pd.read_csv(METADATA_1_CSV, index_col=0)
    leave_order = tmp_meta.loc[leave_species, 'order_chinese_new']
    if leave_order in (['翼手目', '鲸目']):
        plot_df, score, ref_weight = bayes_predict(
            data_dic, leave_species, start_k=10, delta=2, steps=20
        )
```
这段代码怎么可能用贝叶斯呢？
3. model.py 中
```python
1. 数据处理模块：
    输入： 1. 训练集序列文件 feature*species parquet 
           2. 训练集物种标签
    输出： 1. 过滤后的突变位点矩阵 （top_k 个特征）
           2. 突变位点评分
2. 预测模块：
    输入：  预测物种序列，预测物种近邻列表
    输出：  预测结果

    预测算法： 
        1. 判断预测物种所在的 目 是否在训练数据中的数目。如果数目 >= 5 并且包含回声+非回声，执行朴素贝叶斯算法， 否则执行下面的评估
        2. 统计预测物种的回声趋同突变数目是否超过阈值，未超过判定为非回声。否则执行下述判定。
        3. 统计近邻列表中的物种的回声趋同数目的最大值，如果 s(x) > 1 判定为回声，否则非回声。
``` 
这说明你完全理解错了。我清清楚楚的告诉你在这里 | CEP 留一验证评估 | `notebook/04_方法对比_v2.ipynb` | 找CEP 留一验证，
你写的是什么呢？

我知道我整个项目做过很多版，有些乱。但你是qwen-3.7max,我一个月充200块钱的AI模型，做成这个样还好意思吹自己是top-agent模型吗？？？？